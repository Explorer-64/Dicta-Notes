
/**
 * Utility for managing session creation and updates
 * This centralizes session management logic to avoid code duplication
 */
export const SessionManager = {
  // Note: saveTranscriptionSession removed - backend now handles session saving automatically
  // when transcription is processed via fire-and-forget architecture
};
